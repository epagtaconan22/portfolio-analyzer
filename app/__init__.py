# This file marks `app` as a regular Python package (not a namespace package).
# Required so Python 3.14's importlib does not raise KeyError during Streamlit
# hot-reload cycles when app.* submodules are reimported.
